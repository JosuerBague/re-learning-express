const app_paths = {
    HOME: '/',
    VIEWS: 'views',
    PUBLIC: 'public',
    NEW: '/new'
}

const views = {
    HOME: 'index',
    FORM: 'form/form'
}

const PORT = 3000;


export {
    app_paths,
    views,
    PORT
}
